# Environment configuration for Dell Pro 16 Dashboard

# Anthropic API key for Claude chatbot
ANTHROPIC_API_KEY=your-api-key-here

# Data paths (optional - defaults to relative paths)
# RAW_DATA_DIR=/path/to/benchmark/data
# DATA_DIR=./data

# Authentication (leave empty to disable password in local dev)
APP_PASSWORD=

# Chatbot API URL (for production with nginx reverse proxy)
# CHATBOT_API_URL=/api
